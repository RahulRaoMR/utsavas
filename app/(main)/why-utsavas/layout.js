import { buildStaticPageMetadata } from "../../../lib/staticPageSeo";

export const metadata = buildStaticPageMetadata("/why-utsavas");

export default function WhyUtsavasLayout({ children }) {
  return children;
}
