import { buildStaticPageMetadata } from "../../../lib/staticPageSeo";

export const metadata = buildStaticPageMetadata("/account-deletion");

export default function AccountDeletionLayout({ children }) {
  return children;
}
